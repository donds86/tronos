# Migrations Firebird do TronFire

Coloque aqui scripts SQL incrementais, por exemplo:

```txt
138.sql
139.sql
140.sql
```

Antes de aplicar uma migration no banco de produção, o TronFire deve gerar backup obrigatório.
